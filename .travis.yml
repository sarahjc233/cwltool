language: python
matrix:
    include:
        - os: osx
          language: generic
          env: TOXENV=py27
install:
  - bash .travis/install.sh
  - pip install 'setuptools>=18.5'
  - pip install .
# command to run tests
script: nosetests


